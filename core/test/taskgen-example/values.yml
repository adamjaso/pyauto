targets:
  deployments:
    name: dep
    list: [east, west]
  apps:
    name: app
    depends: deployments
    list: [web, api]
  services:
    name: [app, srv]
    depends: deployments
    map:
    - web: [redis]
    - api: [postgres]

